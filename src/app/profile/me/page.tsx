import { FormProfile } from '@/components';
import { Profile } from '@/templates';

const MePage = () => {
  return (
    <Profile>
      <FormProfile />
    </Profile>
  );
};

export default MePage;
