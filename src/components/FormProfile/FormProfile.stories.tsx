import { Meta } from '@storybook/react';
import { FormProfile } from '.';

export default {
  title: 'Components/FormProfile',
  component: FormProfile,
} as Meta;

export const Default = () => <FormProfile />;
