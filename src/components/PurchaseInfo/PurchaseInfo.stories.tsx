import { Meta } from '@storybook/react';
import { PurchaseInfo } from '.';

export default {
  title: 'Components/PurchaseInfo',
  component: PurchaseInfo,
} as Meta;

export const Default = () => <PurchaseInfo />;
